<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.consultant">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.ReleaseActivity"></activity>
        <activity android:name=".former.material_add" />
        <activity android:name=".former.material" />
        <activity android:name=".activities.LoginAndRegisterActivity" />
        <activity android:name=".former.note_bill" />
        <activity android:name=".former.client_add" />
        <activity android:name=".former.alert_add" />
        <activity android:name=".former.income" />
        <activity android:name=".activities.AcountActivity" />
        <activity android:name=".former.voice2text" />
        <activity android:name=".former.client" />
        <activity android:name=".former.alert" />
        <activity android:name=".former.free" />
        <activity
            android:name=".activities.EditActivity"
            android:label="@string/title_activity_edit"
            android:parentActivityName=".activities.MainActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activities.AdviceActivity" />
        <activity android:name=".former.settings" />
        <activity android:name=".former.corporation" />
        <activity android:name=".former.help" />
        <activity android:name=".former.scan" />
        <activity android:name=".former.publish" />
        <activity android:name=".former.collect" />
        <activity android:name=".former.attention" />
        <activity
            android:name=".activities.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>